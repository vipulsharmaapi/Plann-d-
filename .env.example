# Copy to .env and fill in from Supabase project settings (Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: Google Places venue search (falls back to free OSM geocoder if unset).
# Google Cloud Console → enable "Places API (New)" → create API key.
VITE_GOOGLE_MAPS_API_KEY=
